"""Instrucción:

Crea lista = []
Usa if not lista: para verificar si está vacía
Si está vacía, muestra "Lista vacía"
Si no, muestra "La lista tiene elementos"

Explicación:
not lista → Verifica si la lista está vacía (False)
"""

lista = []

if not lista:
    print("Lista vacía")
else:
    print("La lista tiene elementos")